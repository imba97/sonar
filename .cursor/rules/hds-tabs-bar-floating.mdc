---
description: HarmonyOS HDS 底部页签悬浮样式与迷你栏规范
alwaysApply: true
---

# HDS 底部页签悬浮样式规范

## 文档要点总览
- 自 HarmonyOS 6.1.0(23) 起，`HdsTabs` 支持页签栏悬浮样式与迷你栏（mini bar）。
- 悬浮页签用于提升内容沉浸感，迷你栏用于承载额外自定义操作区域。
- 悬浮效果与沉浸光感可组合使用，建议按设备能力做材质等级自适配。

## 适用范围
- 组件：`HdsTabs`。
- 场景：底部导航需要悬浮展示，内容需延伸至底部区域。
- 版本：HarmonyOS 6.1.0(23)+。

## 约束条件
- 当前悬浮样式仅支持以下页签栏样式配置：
  - `BottomTabBarStyle`
  - `CustomBuilder`

## 启用悬浮样式的必要条件
- 开启内容与底栏叠加：`.barOverlap(true)`
- 设置底部位置：`.barPosition(BarPosition.End)`
- 设置横向布局：`.vertical(false)`
- 配置悬浮样式：`.barFloatingStyle({...})`

## `barFloatingStyle` 参数清单
- `barWidth`：分档宽度配置（如 `smallWidth` / `mediumWidth` / `largeWidth`）。
- `barBottomMargin`：页签栏与底部间距，控制悬浮高度。
- `gradientMask`：渐变蒙层（`maskColor`、`maskHeight`），用于衔接内容与导航层次。
- `systemMaterialEffect`：沉浸光感材质参数（`materialType`、`materialLevel`）。
- `miniBar`：迷你栏配置，可通过 `miniBarBuilder` 注入自定义 UI。

## 迷你栏设计建议
- 迷你栏与页签栏等高且水平对齐，支持展开与折叠两种状态。
- 迷你栏内容应聚焦高频轻操作，不承载复杂表单或重交互。
- 迷你栏按钮数量建议精简，避免与页签主导航语义冲突。

## 播放状态 UI 唯一入口（强制，避免重复底栏）

根 Tab 页（`FloatingTabBar` + `HdsTabs.barFloatingStyle.miniBar`）已承载**右侧**播放状态迷你栏时，**禁止**再叠加独立播放条或左侧悬浮球：

- **禁止**在 `Index.ets`（或同级根页面）用 `Stack` 叠加 `MiniPlayerBar`、自定义 `Row` 胶囊条、左侧 `coverDisc` 悬浮球等第二套播放 UI。
- **禁止**「HDS miniBar + `MiniPlayerBar`」双轨并存；用户只保留 **tab 栏右侧 miniBar** 一处。
- 播放态文案、封面、播放/下一首/循环等控件，**只写在** `FloatingTabBar.miniBarBuilder()`（或等价单一 Builder），勿拆到页级 overlay。
- 无曲目时的默认图标使用 `dock_music_note`（音符），勿用应用 Logo / `lib_icon_12` 等地球/品牌图冒充音乐占位。
- 修改播放 UI 前先确认：是否会在底部出现**两个圆角条或两个圆形控件**；若是，则属于回归，必须删掉 overlay 方案。

## miniBar 折叠球形居中（强制）

HDS 折叠后迷你栏宽度约 `56vp`（与 `FLOATING_TAB_BAR_HEIGHT` 一致）。**禁止**在折叠态仍使用「左内边距 + 横向 `Row` + 42vp 封面」的展开布局，否则系统裁切圆球时只会露出 Row 左侧，封面/音符会**整体偏右**。

- 用 `onAreaChange` 测量 miniBar 实际宽度，宽度 `≤ 60vp` 时走**折叠布局**：固定 `56×56` 的 `Stack(Alignment.Center)` + 封面尺寸 `FLOATING_TAB_BAR_HEIGHT`，**无** `padding-left`。
- 宽度变大后再切换为展开 `Row`（文案 + 控制按钮 + 横向 `padding`）。
- 折叠与展开须拆成两个 `@Builder`，勿在同一 `Row` 上仅靠 `clip` 凑合。

**正确结构（根页）**

```typescript
Navigation(...) {
  FloatingTabBar() // 内含 HdsTabs + miniBarBuilder
}
// 不要：Stack { Navigation(...); MiniPlayerBar(); }
```

**错误结构（曾导致重复底栏）**

```typescript
Stack({ alignContent: Alignment.Bottom }) {
  Navigation(...) { FloatingTabBar() }
  MiniPlayerBar() // ❌ 与 miniBar 重复
}
```

## 推荐材质策略
- 默认使用系统自适应：
  - `materialType: hdsMaterial.MaterialType.ADAPTIVE`
  - `materialLevel: hdsMaterial.MaterialLevel.ADAPTIVE`
- 当有强视觉定制诉求时，再结合设备能力（是否支持 `IMMERSIVE`）决定等级。

## 参考代码骨架（ArkTS）
```typescript
HdsTabs({ controller: this.controller }) {
  // TabContent...
}
.barOverlap(true)
.barPosition(BarPosition.End)
.vertical(false)
.barFloatingStyle({
  barWidth: { smallWidth: 200, mediumWidth: 300, largeWidth: 400 },
  barBottomMargin: 28,
  gradientMask: { maskColor: '#66F1F3F5', maskHeight: 92 },
  systemMaterialEffect: {
    materialType: hdsMaterial.MaterialType.ADAPTIVE,
    materialLevel: hdsMaterial.MaterialLevel.ADAPTIVE
  },
  miniBar: {
    miniBarBuilder: () => this.miniBarBuilder()
  }
})
```

## 验收检查项
- 悬浮栏出现后，内容区域是否连续且无遮挡关键交互。
- 迷你栏折叠/展开切换是否平滑，且不影响页签可点击性。
- 深色/浅色模式下蒙层与材质对比度是否可读。
- 小屏与大屏（含折叠态）下宽度分档是否符合预期。

## 参考文档
- https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ui-design-hds-tabs-bar-floating
